# Скрипт очистки базы данных и файловой системы

Этот скрипт предназначен для автоматической очистки таблиц в базе данных MySQL и файлов в указанных папках с возможностью последующего копирования файлов.

## Возможности

- ✅ Очистка таблиц MySQL (TRUNCATE)
- ✅ Удаление таблиц MySQL (DROP)
- ✅ Очистка содержимого папок
- ✅ Копирование файлов после очистки
- ✅ Резервное копирование (можно отключить)
- ✅ Копирование от имени другого пользователя
- ✅ Подтверждение опасных операций
- ✅ Настройки через INI-файл

## Установка

1. **Установите зависимости:**
```bash
pip3 install pymysql cryptography
```

2. **Создайте файл настроек `settings.ini`:**
```bash
cp settings.ini.example settings.ini
```

3. **Настройте права доступа:**
```bash
chmod 600 settings.ini  # защищаем файл с паролями
chmod +x cleaner.py
```

## Настройка

Отредактируйте файл `settings.ini`:

### Секция [database]
```ini
[database]
host = localhost
user = your_username
password = your_password
database_name = your_database
mode = drop_all  ; drop_all, drop_list, truncate
tables = table1, table2, logs
```

### Секция [folders]
```ini
[folders]
clean = /tmp/cache, /var/log/app
copy_sources = /path/to/source1, /path/to/source2
copy_destinations = /tmp/cache, /var/log/app
copy_user = www-data  ; опционально: пользователь для копирования
```

### Секция [backup]
```ini
[backup]
enable = true  ; включить/отключить резервное копирование
backup_dir = /tmp/backup  ; папка для бэкапов (пусто = автоматическая)
```

### Секция [security]
```ini
[security]
confirm_destructive_operations = true  ; запрашивать подтверждение
```

## Режимы работы с БД

- **drop_all** - удалить ВСЕ таблицы в базе
- **drop_list** - удалить только указанные таблицы  
- **truncate** - очистить только указанные таблицы

## Использование

```bash
# Базовый запуск
./cleaner.py

# Запуск без подтверждений (если confirm_destructive_operations=false)
./cleaner.py

# Просмотр логов в реальном времени
./cleaner.py | tee cleanup.log
```

## Пример workflow

1. Создание резервной копии (если включено)
2. Очистка таблиц БД согласно настройкам
3. Очистка указанных папок
4. Копирование файлов из источников в очищенные папки

## Копирование от другого пользователя

Если нужно копировать файлы с сохранением прав определенного пользователя:

1. Укажите пользователя в `copy_user`
2. Убедитесь, что текущий пользователь имеет права sudo для этого пользователя
3. Добавьте в sudoers (если нужно):
```bash
# /etc/sudoers.d/cleaner
your_user ALL=(www-data) NOPASSWD: /bin/cp, /bin/mkdir, /usr/bin/rsync
```

## Безопасность

- 🔐 Пароли БД хранятся в отдельном файле
- ⚠️ Подтверждение для опасных операций
- 📁 Резервные копии создаются автоматически
- 🔒 Файл настроек защищен правами 600

## Логирование

Скрипт выводит подробные логи в консоль. Для сохранения логов:

```bash
./cleaner.py 2>&1 | tee cleanup_$(date +%Y%m%d_%H%M%S).log
```

## Ошибки и устранение неполадок

### Ошибка подключения к БД
- Проверьте параметры подключения в settings.ini
- Убедитесь, что MySQL сервер запущен
- Проверьте права пользователя БД

### Ошибки доступа к файлам
- Проверьте права на папки
- Для копирования от другого пользователя настройте sudo
- Убедитесь, что папки существуют

### Проблемы с резервным копированием
- Проверьте доступное место на диске
- Убедитесь в правах на запись в backup_dir

## Важные предупреждения

⚠️ **Режим `drop_all` необратимо удаляет ВСЕ таблицы в базе!**

⚠️ **Всегда тестируйте скрипт на тестовой среде перед использованием в продакшене**

⚠️ **Убедитесь, что резервные копии создаются корректно**

## Лицензия

MIT License